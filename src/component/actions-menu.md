---
description: Create actions menu with options for size `defult` or `sm`. Default size is sm.
scope: html
---

```html
<sds-actions-menu
  size="${1|sm|}"
  [model]="${2|menu|}"
  (clicks)="${3|event|}"
></sds-actions-menu>
```